# Python dependencies for video processing pipeline
# GPU-accelerated AI models for transcription and summarization

# PyTorch (CUDA 11.8)
torch==2.1.0
torchaudio==2.1.0

# Transformer models (Llama)
transformers==4.35.2
accelerate==0.25.0
bitsandbytes==0.41.3
sentencepiece==0.1.99
protobuf==3.20.3

# WhisperX for transcription with diarization
# Note: Install from git - see Dockerfile
# git+https://github.com/m-bain/whisperx.git

# Content moderation
nudenet==2.0.9
opencv-python-headless==4.8.1.78
pillow==10.1.0

# AWS SDK (includes DynamoDB)
boto3==1.34.10
